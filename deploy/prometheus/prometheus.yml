global:
  scrape_interval: 15s

scrape_configs:
  - job_name: aegis
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials_file: /run/secrets/prometheus_token
    static_configs:
      - targets: ["backend:8000"]
