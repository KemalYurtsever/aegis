apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: aegis
spec:
  replicas: 1
  selector:
    matchLabels: {app: aegis-backend}
  template:
    metadata:
      labels: {app: aegis-backend}
    spec:
      containers:
        - name: backend
          image: ghcr.io/replace-me/aegis-backend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - {name: http, containerPort: 8000}
          env:
            - {name: DATABASE_URL, value: "sqlite:////data/monitoring.db"}
            - {name: AEGIS_PROMETHEUS_TOKEN_FILE, value: /run/secrets/prometheus-token}
            - name: AEGIS_SMTP_PASSWORD
              valueFrom: {secretKeyRef: {name: aegis-secrets, key: smtp-password}}
            - name: AEGIS_TEAMS_WEBHOOK_URL
              valueFrom: {secretKeyRef: {name: aegis-secrets, key: teams-webhook-url}}
            - name: AEGIS_TWILIO_ACCOUNT_SID
              valueFrom: {secretKeyRef: {name: aegis-secrets, key: twilio-account-sid}}
            - name: AEGIS_TWILIO_AUTH_TOKEN
              valueFrom: {secretKeyRef: {name: aegis-secrets, key: twilio-auth-token}}
            - name: AEGIS_SNMP_COMMUNITY
              valueFrom: {secretKeyRef: {name: aegis-secrets, key: snmp-community}}
          volumeMounts:
            - {name: data, mountPath: /data}
            - {name: prometheus-token, mountPath: /run/secrets/prometheus-token, subPath: prometheus-token, readOnly: true}
          readinessProbe: {httpGet: {path: /api/health, port: http}, initialDelaySeconds: 5, periodSeconds: 10}
          livenessProbe: {httpGet: {path: /api/health, port: http}, initialDelaySeconds: 15, periodSeconds: 20}
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: "1", memory: 512Mi}
      volumes:
        - name: data
          persistentVolumeClaim: {claimName: aegis-data}
        - name: prometheus-token
          secret: {secretName: aegis-secrets}
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: aegis
spec:
  selector: {app: aegis-backend}
  ports:
    - {name: http, port: 8000, targetPort: http}
